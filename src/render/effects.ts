import type { FxState } from '../core/types';

// Phase 8 will flesh these out — stubs keep the interface stable
export class Effects {
  consume(_fx: FxState, _ctx: CanvasRenderingContext2D): void {
    // Particles, screen shake, chain flash added in Phase 8
  }
}
